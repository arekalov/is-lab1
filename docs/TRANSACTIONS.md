# Документация: Транзакции в приложении

## ✅ Реализованные транзакции

### 1. Аннотация `@Transactional`

Все операции модификации данных теперь обернуты в транзакции с помощью аннотации `@Transactional` из Jakarta EE.

### 2. Тип транзакции: `REQUIRED`

```java
@Transactional(Transactional.TxType.REQUIRED)
```

**Поведение:**
- Если транзакция уже существует - использует её
- Если транзакции нет - создает новую
- Это **стандартный и наиболее распространенный тип**

### 3. Методы с транзакциями

#### FlatService:
- ✅ `createFlat(Flat flat)` - создание квартиры
- ✅ `updateFlat(Long id, Flat updatedFlat)` - обновление квартиры
- ✅ `deleteFlat(Long id)` - удаление квартиры

#### HouseService:
- ✅ `createHouse(CreateHouseRequest request)` - создание дома
- ✅ `updateHouse(Long id, CreateHouseRequest request)` - обновление дома
- ✅ `deleteHouse(Long id)` - удаление дома (каскадное удаление квартир)

### 4. Методы БЕЗ транзакций (только чтение)

Методы чтения данных **НЕ требуют транзакций**, так как они не изменяют состояние БД:

- `getAllFlats()`, `getFlatById()`, `findByName()` и т.д.
- `getAllHouses()`, `getHouseById()` и т.д.

**Преимущество:** Лучшая производительность, меньше блокировок.

---

## 🔄 Как работают транзакции

### Автоматический Commit/Rollback

JTA (Jakarta Transactions API) автоматически управляет транзакциями:

```java
@Transactional(Transactional.TxType.REQUIRED)
public Flat createFlat(Flat flat) {
    // Транзакция начинается автоматически
    
    flat.setCreationDate(LocalDateTime.now());
    validateFlat(flat);
    
    // Сохранение в БД
    Flat saved = flatRepository.save(flat);
    
    // Если всё OK - автоматический COMMIT
    // Если Exception - автоматический ROLLBACK
    
    return saved;
}
```

### Пример: Каскадное удаление дома

```java
@Transactional(Transactional.TxType.REQUIRED)
public boolean deleteHouse(Long id) {
    // ВСЁ в одной транзакции:
    
    // 1. Проверка количества квартир
    long flatsCount = houseRepository.getFlatsCount(id);
    
    // 2. Удаление дома (каскадно удаляет квартиры)
    boolean deleted = houseRepository.deleteById(id);
    
    // 3. Отправка WebSocket уведомления
    webSocketService.notifyHouseUpdate("DELETE", id);
    
    // Если что-то пошло не так - ВСЁ откатится
    return deleted;
}
```

---

## 📊 Уровни изоляции транзакций

### Текущая конфигурация

По умолчанию используется уровень изоляции **READ_COMMITTED** (настройка БД PostgreSQL).

### Доступные уровни изоляции:

| Уровень | Dirty Read | Non-Repeatable Read | Phantom Read |
|---------|------------|---------------------|--------------|
| READ_UNCOMMITTED | ❌ Возможен | ❌ Возможен | ❌ Возможен |
| **READ_COMMITTED** | ✅ Невозможен | ❌ Возможен | ❌ Возможен |
| REPEATABLE_READ | ✅ Невозможен | ✅ Невозможен | ❌ Возможен |
| SERIALIZABLE | ✅ Невозможен | ✅ Невозможен | ✅ Невозможен |

### Когда менять уровень изоляции?

**Сейчас используется:** READ_COMMITTED (по умолчанию PostgreSQL)
- ✅ Подходит для большинства операций
- ✅ Хорошая производительность
- ✅ Предотвращает dirty reads

**Когда нужен SERIALIZABLE:**
- 🔒 Критичные операции с проверкой уникальности
- 🔒 Импорт данных с бизнес-ограничениями
- 🔒 Операции, где важна полная консистентность

### Как изменить уровень изоляции (для будущего)

```java
@Transactional(
    value = Transactional.TxType.REQUIRED,
    // Для JPA можно настроить через EntityManager
)
public Flat createFlatWithSerializable(Flat flat) {
    // Можно настроить на уровне persistence.xml или программно
    entityManager
        .createNativeQuery("SET TRANSACTION ISOLATION LEVEL SERIALIZABLE")
        .executeUpdate();
    
    // Критичная операция
}
```

**Примечание:** В Jakarta EE изоляция обычно настраивается на уровне БД или через EclipseLink properties.

---

## 🎯 Преимущества текущей реализации

### 1. ✅ Атомарность (Atomicity)
Операция либо выполняется полностью, либо не выполняется вообще.

**Пример:** При создании квартиры с вложенным домом:
- Либо создаются и квартира, и дом
- Либо не создается ничего (при ошибке)

### 2. ✅ Согласованность (Consistency)
БД всегда остается в корректном состоянии.

**Пример:** При каскадном удалении дома:
- Сначала удаляются квартиры
- Потом удаляется дом
- Не остается "висящих" ссылок

### 3. ✅ Изоляция (Isolation)
Транзакции не мешают друг другу.

**Пример:** Два пользователя создают квартиры одновременно:
- Каждая операция видит консистентное состояние БД
- Нет конфликтов при записи

### 4. ✅ Долговечность (Durability)
После commit данные сохранены навсегда.

---

## 🧪 Тестирование транзакций (для JMeter)

### Сценарий 1: Проверка rollback при ошибке

```
1. Начать создание квартиры с неверными данными
2. Проверить, что квартира НЕ создана
3. Проверить, что связанные объекты НЕ созданы
```

### Сценарий 2: Конкурентное создание

```
1. 10 пользователей одновременно создают квартиры
2. Проверить, что все квартиры созданы корректно
3. Проверить отсутствие дубликатов
4. Проверить целостность связей
```

### Сценарий 3: Каскадное удаление

```
1. Создать дом с 5 квартирами
2. Удалить дом
3. Проверить, что все квартиры также удалены
4. Проверить отсутствие orphan записей
```

---

## 📝 Дальнейшие улучшения

### Для Лаб2 Этап 5 (JMeter):

1. **Настроить оптимальный уровень изоляции** для каждого типа операций
2. **Добавить retry логику** для конфликтов при SERIALIZABLE
3. **Оптимизировать блокировки** для массовых операций
4. **Добавить мониторинг** транзакций (логирование, метрики)

### Потенциальные проблемы и решения:

| Проблема | Решение |
|----------|---------|
| Deadlock | Retry с экспоненциальной задержкой |
| Long transactions | Разбить на более мелкие |
| Lock timeout | Увеличить timeout в persistence.xml |
| Phantom reads | Использовать SERIALIZABLE для критичных операций |

---

## 🔗 Связанные файлы

- `src/main/java/com/arekalov/islab1/service/FlatService.java` - сервис квартир
- `src/main/java/com/arekalov/islab1/service/HouseService.java` - сервис домов
- `src/main/resources/META-INF/persistence.xml` - конфигурация JPA
- `src/main/java/com/arekalov/islab1/repository/FlatRepository.java` - репозиторий квартир
- `src/main/java/com/arekalov/islab1/repository/HouseRepository.java` - репозиторий домов
- `scripts/deploy-local.sh` - локальный деплой
- `scripts/deploy-remote.sh` - удаленный деплой

---

**Статус:** ✅ Этап 1 выполнен  
**Дата:** 2025-12-06  
**Следующий этап:** Этап 3 - Ограничения уникальности
