# 🚀 Скрипты для деплоя и подключения

## 📁 Структура скриптов

```
scripts/
├── deploy.sh       # Деплой приложения на сервер
├── connect.sh      # Подключение к серверу с проброской портов
├── clean_server.sh # Очистка сервера
└── start_server.sh # Запуск WildFly (копируется на сервер)
```

## 🔧 Использование

### 1. Деплой приложения
```bash
./scripts/deploy.sh
```
Этот скрипт:
- 📦 Собирает проект (`./gradlew clean build`)
- 🧹 Очищает сервер
- ⏹️ Останавливает Java процессы
- 🗑️ Удаляет логи и временные файлы
- 📤 Загружает WAR файл на сервер
- 💡 Показывает инструкции для подключения

### 2. Подключение к серверу
```bash
./scripts/connect.sh
```
Автоматически подключается к серверу с проброской портов:
- `8080` → WildFly HTTP (приложение)
- `28600` → WildFly Admin Console
- `28603` → WildFly Management Interface

### 3. Запуск сервера (на удаленной машине)
После подключения к серверу:
```bash
./start_server.sh
```

## 🌐 Доступные URL после подключения

| Сервис | URL | Описание |
|--------|-----|----------|
| **Приложение** | http://localhost:8080/is-lab1 | REST API приложения |
| **WildFly Admin** | http://localhost:28600 | Административная консоль |
| **Management** | http://localhost:28603 | Management интерфейс |

## 🎯 API Endpoints

### Дома (Houses)
- `GET /is-lab1/api/houses` - получить все дома
- `GET /is-lab1/api/houses/{id}` - получить дом по ID
- `POST /is-lab1/api/houses` - создать дом
- `PUT /is-lab1/api/houses/{id}` - обновить дом
- `DELETE /is-lab1/api/houses/{id}` - удалить дом (каскадно с квартирами)

### Квартиры (Flats)
- `GET /is-lab1/api/flats` - получить все квартиры
- `GET /is-lab1/api/flats/{id}` - получить квартиру по ID
- `POST /is-lab1/api/flats` - создать квартиру
- `PUT /is-lab1/api/flats/{id}` - обновить квартиру
- `DELETE /is-lab1/api/flats/{id}` - удалить квартиру

## 🔍 Примеры использования

### Создание дома
```bash
curl -X POST http://localhost:8080/is-lab1/api/houses \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Новый дом",
    "year": 2023,
    "numberOfFlatsOnFloor": 4
  }'
```

### Получение всех домов
```bash
curl http://localhost:8080/is-lab1/api/houses
```

### Создание квартиры
```bash
curl -X POST http://localhost:8080/is-lab1/api/flats \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Квартира 1",
    "area": 50,
    "price": 5000000,
    "balcony": true,
    "timeToMetroOnFoot": 10,
    "numberOfRooms": 2,
    "livingSpace": 30,
    "furnish": "FINE",
    "view": "PARK",
    "coordinates": {
      "x": 100,
      "y": 200
    },
    "houseId": 1
  }'
```

## 🏗️ Архитектура

Проект использует:
- **Нативный EclipseLink API** (без JPA)
- **Без транзакций** (auto-commit режим)
- **WildFly** как сервер приложений
- **PostgreSQL** как база данных
- **JAX-RS** для REST API

## 🔧 Техническая информация

- **Java**: OpenJDK 17+
- **Build Tool**: Gradle
- **Server**: WildFly Preview 26.1.3
- **Database**: PostgreSQL
- **ORM**: EclipseLink Native API
- **Logging**: java.util.logging
