# Информационные системы
## Лабораторная работа #1-2

### Задание
Разработать REST API по варианту, реализующий доступ к базе данных с поддержкой транзакций и массового импорта.

Вариант: Квартиры и дома

### Технологический стек
- Java 17
- Jakarta EE 10 (JPA, JTA, CDI, JAX-RS)
- EclipseLink 4.0.2 (JPA Provider)
- WildFly 26.1.3
- PostgreSQL
- Gradle
- Lombok

---

## 🚀 Быстрый старт

### Локальный запуск
```bash
./scripts/deploy-local.sh
```

### Удаленный деплой (на сервер ITMO)
```bash
./scripts/deploy-remote.sh
```

### Проверка конфигурации сервера
```bash
./scripts/check-remote-config.sh
```

---

## 📊 API Endpoints

#### Квартиры (Flats)

`GET /api/flats` - получение списка всех квартир с пагинацией
```
Параметры:
- page (integer) - номер страницы
- size (integer) - размер страницы

Пример: GET /api/flats?page=0&size=10
```

`GET /api/flats/{id}` - получение квартиры по ID
```
Пример: GET /api/flats/1
```

`POST /api/flats` - создание новой квартиры
```
Body:
{
    "name": "Квартира 1",
    "coordinates": {
        "x": 1.0,
        "y": 2.0
    },
    "area": 100,
    "numberOfRooms": 3,
    "furnish": "DESIGNER",
    "view": "YARD",
    "house": {
        "name": "Дом 1",
        "year": 2020,
        "numberOfFloors": 5
    }
}
```

`PUT /api/flats/{id}` - обновление квартиры
```
Пример: PUT /api/flats/1
Body: аналогично POST
```

`DELETE /api/flats/{id}` - удаление квартиры
```
Пример: DELETE /api/flats/1
```

#### Дома (Houses)

`GET /api/houses` - получение списка всех домов
```
Пример: GET /api/houses
```

`GET /api/houses/{id}` - получение дома по ID
```
Пример: GET /api/houses/1
```

`POST /api/houses` - создание нового дома
```
Body:
{
    "name": "Дом 1",
    "year": 2020,
    "numberOfFloors": 5
}
```

`PUT /api/houses/{id}` - обновление дома
```
Пример: PUT /api/houses/1
Body: аналогично POST
```

`DELETE /api/houses/{id}` - удаление дома
```
Пример: DELETE /api/houses/1
```

---

## 🏗️ Архитектура

### Структура проекта
```
src/main/java/com/arekalov/islab1/
├── config/          # Конфигурация (CDI, CORS, Jackson)
├── controller/      # REST контроллеры (JAX-RS)
├── dto/
│   ├── request/     # DTO для запросов (POST, PUT)
│   ├── response/    # DTO для ответов (GET)
│   └── websocket/   # DTO для WebSocket
├── entity/          # JPA Entity (Flat, House, Coordinates)
├── exception/       # Exception mappers
├── mapper/          # Конвертация Entity ↔ DTO
├── repository/      # Слой доступа к данным (JPA)
├── service/         # Бизнес-логика + транзакции
└── websocket/       # WebSocket endpoints
```

### Слои приложения
```
Controller (REST) → Service (@Transactional) → Repository (JPA) → Database
        ↓                ↓                           ↓
    Mapper          Business Logic            EntityManager
```

---

## 🔄 Транзакции

Все операции модификации данных обернуты в JTA транзакции:

- **CREATE** - `@Transactional(TxType.REQUIRED)`
- **UPDATE** - `@Transactional(TxType.REQUIRED)`
- **DELETE** - `@Transactional(TxType.REQUIRED)`

Подробнее: см. `docs/TRANSACTIONS.md`

---

## 📁 Скрипты

### Деплой
- `scripts/deploy-local.sh` - локальный запуск с WildFly
- `scripts/deploy-remote.sh` - удаленный деплой на сервер ITMO

### Утилиты
- `scripts/check-remote-config.sh` - проверка конфигурации сервера
- `scripts/clean_server.sh` - очистка сервера от временных файлов

### SQL
- `sql/create-tables.sql` - создание таблиц БД
- `sql/migration-add-floor.sql` - миграция добавления поля floor

---

## 📝 Документация

- `PLAN_LAB2.md` - план работ для Лабораторной 2
- `docs/TRANSACTIONS.md` - документация по транзакциям
- `README.md` - этот файл

---

## 🛠️ Сборка и развертывание

### Сборка проекта
```bash
./gradlew clean build
```

### Локальный запуск
```bash
./scripts/deploy-local.sh
```

Приложение будет доступно:
- API: http://localhost:8080/is-lab1/api
- WildFly Admin: http://localhost:9990

### Удаленный деплой
```bash
./scripts/deploy-remote.sh
```

После деплоя сервер будет доступен через проброс портов:
- API: http://localhost:8080/is-lab1/api
- WildFly Admin: http://localhost:28600

---

## ✨ Особенности реализации

### JPA + EclipseLink
- Использование JPA вместо нативного API
- EclipseLink как JPA провайдер
- Автоматическое создание/обновление схемы БД

### Транзакции
- JTA транзакции для всех операций модификации
- Автоматический rollback при ошибках
- Каскадное удаление связанных объектов

### DTO Pattern
- Разделение Request/Response DTO
- Централизованные Mapper'ы
- Builder pattern для создания Entity

### WebSocket
- Реал-тайм уведомления о изменениях
- Уведомления о создании/обновлении/удалении

---

## 🎯 Статус разработки

- ✅ REST API для Flat и House
- ✅ JPA с EclipseLink
- ✅ Транзакции (JTA)
- ✅ DTO Mappers
- ✅ WebSocket уведомления
- ✅ Деплой скрипты
- 🔄 Массовый импорт (в процессе)
- 🔄 Ограничения уникальности (в процессе)
- 📅 JMeter тестирование (запланировано)

---

## 👨‍💻 Автор
Лабораторная работа по дисциплине "Информационные системы"  
ITMO University